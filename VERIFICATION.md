# Verification Log

Rule: a feature is not "done" -- in chat, in commit messages, or in your
own head -- until an entry below exists with REAL output pasted in.
"The build passed" and "I don't see an error" do not count. For anything
touching money or order execution, two consecutive real runs showing the
expected state change (like the Current Opportunities insert-then-update
test) are required, not one.

Copy the block below per feature.

---

## Feature: <name>
- Verified by: <exact command or SQL query run>
- Output seen: <paste the actual real output -- not a summary of it>
- Verified on: <date>
- Confidence: <single run | two consecutive runs (state-transition proof)>

---
